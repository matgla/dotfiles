(deflisten workspaces
  `$HOME/.config/eww/common/scripts/hyprland_workspace.py`)

(defvar cpu_temperature_field "ASUS_WMI_SENSORS_CPU_TEMPERATURE")
(defvar battery_enable false)

(include "common/eww.yuck")
